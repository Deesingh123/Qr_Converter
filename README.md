# Industrial QR Generator

A Windows-based industrial QR code generator that:
- Accepts barcode scanner or manual input
- Validates length & characters
- Prevents duplicate QR generation
- Auto-saves QR images
- Supports optional physical printing
- Packaged as a portable EXE

## How to Run (Developer)
```bash
pip install -r requirements.txt
python main.py
